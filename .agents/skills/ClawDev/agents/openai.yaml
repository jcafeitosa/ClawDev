interface:
  display_name: "ClawDev"
  short_description: "Repo-specific patterns and workflows for ClawDev"
  default_prompt: "Use the ClawDev repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true